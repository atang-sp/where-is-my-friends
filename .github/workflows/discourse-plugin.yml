name: Discourse Plugin

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    uses: discourse/.github/.github/workflows/discourse-plugin.yml@v1
    with:
      core_ref: tests-passed

  latest:
    uses: discourse/.github/.github/workflows/discourse-plugin.yml@v1
